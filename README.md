# Rare Workspace (Split Repos)

该目录已按实施计划切换为三仓工作区（立即切换，无旧命名兼容层）：

- `rare-identity-core/`
- `rare-sdk-python/`
- `rare-thirdparty-moltbook-example/`

## Quick Validation

```bash
./scripts/test_all.sh
```
