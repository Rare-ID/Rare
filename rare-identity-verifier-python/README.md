# rare-identity-verifier-python

Rare 的 Python verifier 包，提供：

- identity attestation 校验
- delegation token 校验
- Rare JWKS 解析

## Install

```bash
pip install rare-identity-verifier
```

本地工作区开发：

```bash
pip install -e .[test]
```
